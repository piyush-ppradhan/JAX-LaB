[build-system]
requires = ["setuptools>=77", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "jax-lab"
dynamic = ["version"]
description = "A JAX-based differentiable Lattice Boltzmann library for single and multiphase flow simulations on CPUs, GPUs and TPUs"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [
  { name = "Piyush Pradhan", email = "pradhanpiyush687@gmail.com" },
]
keywords = ["lattice-boltzmann", "jax", "cfd", "multiphase", "differentiable", "gpu"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
  "jax==0.10.0",
  "numpy",
  "orbax-checkpoint",
]

[project.optional-dependencies]
# I/O, visualization and geometry utilities (imported lazily in jax_lab.core.utils)
io = ["pyvista", "matplotlib", "Rtree", "trimesh", "h5py"]
# Accelerator backends; the jax extra manages the compiled runtime packages
cuda13 = ["jax[cuda13]==0.10.0"]
cuda12 = ["jax[cuda12]==0.10.0"]
cuda13-local = ["jax[cuda13-local]==0.10.0"]
cuda12-local = ["jax[cuda12-local]==0.10.0"]
tpu = ["jax[tpu]==0.10.0"]
rocm = ["jax[rocm7-local]==0.10.0"]
# Development and documentation tooling
dev = ["build", "twine", "ruff", "pytest"]
docs = ["zensical", "mkdocstrings-python"]

[project.urls]
Repository = "https://github.com/piyush-ppradhan/JAX-LaB"
Paper = "https://doi.org/10.1029/2025MS005313"

[tool.setuptools.packages.find]
include = ["jax_lab*"]

[tool.setuptools_scm]
# Versions come from git tags (v0.1.0 -> 0.1.0). Untagged commits produce
# PEP 440 dev versions without a local segment so they remain uploadable
# to TestPyPI (PyPI/TestPyPI reject local version segments like +g<hash>).
local_scheme = "no-local-version"

[tool.pytest.ini_options]
testpaths = ["tests"]
